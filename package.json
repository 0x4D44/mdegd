{
  "name": "erics-birthday-quiz",
  "version": "0.1.0",
  "description": "A March 21st birthday quiz for Eric — with difficulty that depends on who you are",
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/vitest run",
    "test:watch": "node --experimental-vm-modules node_modules/.bin/vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "jsdom": "^26.0.0"
  }
}
